sudo: false
language: node_js
node_js:
  - 12
cache:
  - npm
branches:
  only:
    - site

script:
  - hexo generate

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: false
  committer_from_gh: true
  target_branch: master
  on:
    branch: site
  local-dir: public

